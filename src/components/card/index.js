import React from 'react';
import './card.css';

const Card = ({}) => {
    return(
        <div className="card" style={{
            height: '420px',
            width: '280px',
        }}>

        </div>
    );
};

export default Card;